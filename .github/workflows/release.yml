name: 🏷️ Release

on:
  push:
    tags:
      - 'v*' # Trigger on version tags (v1.0.0, v1.1.0, etc.)
  workflow_dispatch: # Manual trigger also available

permissions:
  contents: write
  packages: write

env:
  CARGO_TERM_COLOR: always

jobs:
  create-release:
    name: 🏷️ Create Release
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: 🏷️ Get tag name
      id: tag
      run: echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
      
    - name: 📝 Create release notes
      run: |
        cat > release_notes.md << 'EOF'
        # 🏷️ Meteora Fee Router ${{ steps.tag.outputs.tag }}
        
        ## ✨ Features
        - **Quote-Only Fee Collection**: Honorary DAMM v2 LP position with strict enforcement
        - **24-Hour Distribution Crank**: Permissionless system with pagination support
        - **Streamflow Integration**: Real-time vesting schedule reading for proportional distribution
        - **Production Ready**: Passing Rust unit tests with comprehensive coverage
        
        ## 🧪 Testing & Quality
        - ✅ **Rust Unit Tests** - Passing with comprehensive coverage
        - ✅ **Integration Validation** - End-to-end scenario validation
        - ✅ **Security Audit** - Built-in security validation and overflow protection
        - ✅ **Performance Optimized** - Efficient compute usage for Solana constraints
        
        ## 📚 Documentation
        - Documentation index and security audit summary
        - Deployment guide and validation scripts
        
        ## 🚀 Deployment
        - Multi-platform deployment scripts (Unix & Windows)
        - Configuration templates for different environments
        - Automated validation and verification tools
        - Production-ready build optimization
        
        ## 🔗 Quick Links
        - [Documentation Index](docs/README.md)
        - [Security Audit Summary](docs/SECURITY_AUDIT_SUMMARY.md)
        
        ---
        
        **Status**: Production Ready 🚀
        EOF
        
    - name: 🏷️ Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.tag.outputs.tag }}
        name: 🏷️ Meteora Fee Router ${{ steps.tag.outputs.tag }}
        body_path: release_notes.md
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
  build-and-package:
    name: 📦 Build & Package
    runs-on: ubuntu-latest
    needs: create-release
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🦀 Setup Rust
      uses: dtolnay/rust-toolchain@stable
      
    - name: 🌐 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        registry-url: 'https://registry.npmjs.org'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🏗️ Build program
      run: |
        # Install Solana CLI
        sh -c "$(curl -sSfL https://release.solana.com/v1.16.25/install)"
        export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
        
        # Install Anchor CLI
        npm install -g @coral-xyz/anchor-cli@0.32.1
        
        # Build with optimization
        anchor build --verifiable
        
    - name: 📦 Package for NPM
      run: |
        # Create NPM package structure
        mkdir -p npm-package
        cp -r target/idl npm-package/
        cp -r target/types npm-package/
        cp README.md npm-package/
        cp LICENSE npm-package/
        
        # Create package.json for NPM
        cat > npm-package/package.json << 'EOF'
        {
          "name": "@ashqking/meteora-fee-router",
          "version": "${{ github.ref_name }}",
          "description": "Production-grade Solana program for automated fee distribution with quote-only accrual",
          "main": "types/meteora_fee_router.ts",
          "types": "types/meteora_fee_router.ts",
          "files": [
            "idl/",
            "types/",
            "README.md",
            "LICENSE"
          ],
          "keywords": [
            "solana",
            "anchor",
            "defi",
            "meteora",
            "fee-distribution",
            "streamflow",
            "damm-v2"
          ],
          "author": "@ashqking",
          "license": "MIT",
          "repository": {
            "type": "git",
            "url": "https://github.com/iamaanahmad/meteora-fee-router.git"
          },
          "peerDependencies": {
            "@coral-xyz/anchor": "^0.32.1",
            "@solana/web3.js": "^1.87.0"
          }
        }
        EOF
        
    - name: 📤 Upload release assets
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        files: |
          target/deploy/*.so
          target/idl/*.json
          npm-package/**
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
